pub fn init_server(file: &Option<String>) {
    // println!("Current directory: {:?}", std::env::current_dir().unwrap());
    println!("Initializing server with config file: {:?}", file);
}

pub fn login(user: &str){
    println!("Hello, {}!", user);
}